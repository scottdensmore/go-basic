Create an interpreter for BASIC. It should accept the source file as the first argument and run the script.

Your target for functionality is 6502 Microsoft BASIC equivalency.

Your code must compile and run on the linux, Windows and macOS.

You can use program.bas to test if this works.

