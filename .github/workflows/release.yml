name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: false

      - name: Run quality gate
        run: make fmt-check vet coverage-check

  build-and-release:
    name: Build and Release
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            asset_name: go-basic-linux-amd64
          - goos: linux
            goarch: arm64
            asset_name: go-basic-linux-arm64
          - goos: windows
            goarch: amd64
            asset_name: go-basic-windows-amd64.exe
          - goos: darwin
            goarch: amd64
            asset_name: go-basic-darwin-amd64
          - goos: darwin
            goarch: arm64
            asset_name: go-basic-darwin-arm64

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache: false

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          go build -v -ldflags "-X main.Version=${{ github.ref_name }}" -o ${{ matrix.asset_name }} ./cmd/go-basic

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: ${{ matrix.asset_name }}
